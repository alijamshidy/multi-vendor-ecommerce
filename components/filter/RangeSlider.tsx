import { Slider } from "../ui/slider";

export default function RangeSlider({ className }: { className?: string }) {
  return (
    <div className={className}>
      <Slider></Slider>
    </div>
  );
}
